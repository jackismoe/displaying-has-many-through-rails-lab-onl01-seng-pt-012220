<%= @patient.name %>
<% @patient.appointments.each do |x| %>
  <% @patient_doctor = @patient.doctors.find(x.doctor_id) %>
  <%= link_to @patient_doctor.name, doctor_path(@patient_doctor) %> - <%= link_to x.appointment_datetime.strftime("%B %d, %Y at %k:%M"), appointment_path(x) %><br>
<% end %>