<%= @doctor.name %><br>
<% @doctor.appointments.each do |x| %>
  <% @doctor_patient = @doctor.patients.find(x.patient_id) %>
  <%= link_to @doctor_patient.name, patient_path(@doctor_patient) %> - <%= link_to x.appointment_datetime.strftime("%B %d, %Y at %k:%M"), appointment_path(x) %><br>
<% end %>